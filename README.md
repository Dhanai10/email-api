# Email API (FastAPI + SendGrid)

## Setup

1. Clone repo
2. Create virtual environment
3. Install dependencies:
   pip install -r requirements.txt

4. Create .env file:
   SENDGRID_API_KEY=your_key
   FROM_EMAIL=your_email

5. Run:
   uvicorn main:app --reload
